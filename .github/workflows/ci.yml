name: CI

on:
  push:
    branches: [master, main]
  pull_request:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        python-version: ['3.10', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Create virtualenv
        run: python -m venv .venv
      - name: Activate venv and install deps (Unix)
        if: runner.os != 'Windows'
        run: |
          source .venv/bin/activate
          pip install numpy pint pytest
      - name: Activate venv and install deps (Windows)
        if: runner.os == 'Windows'
        run: |
          .venv\Scripts\activate
          pip install numpy pint pytest
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      - name: Build and install
        uses: PyO3/maturin-action@v1
        with:
          command: develop
          args: --release
      - name: Run Rust tests
        run: cargo test --workspace
      - name: Test Python import (Unix)
        if: runner.os != 'Windows'
        run: |
          source .venv/bin/activate
          python -c "from metrust.calc import potential_temperature; from metrust.units import units; print('OK:', potential_temperature(1000 * units.hPa, 25 * units.degC))"
      - name: Test Python import (Windows)
        if: runner.os == 'Windows'
        run: |
          .venv\Scripts\activate
          python -c "from metrust.calc import potential_temperature; from metrust.units import units; print('OK:', potential_temperature(1000 * units.hPa, 25 * units.degC))"
